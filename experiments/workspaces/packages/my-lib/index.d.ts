/**
 * Important lib function
 * 
 * 
 * **Example Usage**
 * ```ts
 *   libraryFunction("Hello");
 * ```
 * 
 * 
 * @param message message to user
 */
export function libraryFunction(message: string): {
  message: string;
  success: boolean;
};

export function fakeFunction() {
  // There is no Spoon!
}

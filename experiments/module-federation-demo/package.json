{
  "name": "module-federation-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n shell,remote -c blue,green \"npm run dev -w shell-static\" \"npm run dev -w remote-js\""
  },
  "workspaces": [
    "libs/*",
    "remotes/*",
    "shells/*"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2",
    "esbuild-loader": "^4.5.0",
    "tsx": "^4.23.13"
  }
}
